{
  "name": "katacut",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.9.0",
  "scripts": {
    "build": "pnpm exec tsc -b",
    "clean": "pnpm -r exec rimraf dist",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm exec tsc -b",
    "typecheck:watch": "pnpm exec tsc -b --watch",
    "dev": "pnpm exec tsc -b --watch",
    "katacut": "node packages/cli/bin/katacut.mjs",
    "kc": "pnpm katacut"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.6",
    "@types/node": "^24.8.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
