// Centralized constants to avoid string literals scattered across the codebase.
// Keep this file minimal and focused on public endpoints and well-known hosts.

// Official MCP Registry
export const REGISTRY_DEFAULT_BASE = 'https://registry.modelcontextprotocol.io' as const;
export const REGISTRY_HOST = 'registry.modelcontextprotocol.io' as const;

// Smithery server URLs
export const SMITHERY_SERVER_HOST = 'server.smithery.ai' as const;
